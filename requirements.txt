# SmartScheduler - dipendenze Python
#
# L'inferenza LLM avviene tramite Google Gemini 2.5 Flash (API remota).
# Prerequisito: impostare la variabile d'ambiente GEMINI_API_KEY con la
# propria chiave ottenuta da https://aistudio.google.com/app/apikey
#
# Installazione:
#     pip install -r requirements.txt

# --- LLM remoto via Google Gemini ---
google-genai

# --- Validazione output strutturato ---
pydantic

# --- Motore di ottimizzazione (modello CP-SAT) ---
ortools
